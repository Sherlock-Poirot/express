<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.express.yto.dao.SysRoleMapper">

    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT sr.role_code
        FROM t_sys_role sr
        INNER JOIN t_sys_user_role sur ON sr.id = sur.role_id
        WHERE sur.user_id = #{userId}
          AND sr.status = 1
    </select>

    <select id="selectByRoleCode" resultType="com.express.yto.model.SysRole">
        SELECT *
        FROM t_sys_role
        WHERE role_code = #{roleCode}
          AND status = 1
    </select>

</mapper>