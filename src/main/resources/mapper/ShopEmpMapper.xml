<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.express.yto.dao.ShopEmpMapper">
  <resultMap id="BaseResultMap" type="com.express.yto.model.ShopEmp">
    <!--@mbg.generated-->
    <!--@Table t_shop_emp-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="code" jdbcType="VARCHAR" property="code" />
    <result column="cust_name" jdbcType="VARCHAR" property="custName" />
    <result column="shop_id" jdbcType="VARCHAR" property="shopId" />
    <result column="shop_name" jdbcType="VARCHAR" property="shopName" />
    <result column="platform" jdbcType="VARCHAR" property="platform" />
    <result column="emp_name" jdbcType="VARCHAR" property="empName" />
    <result column="emp_type" jdbcType="VARCHAR" property="empType" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, code, cust_name, shop_id, shop_name, platform, emp_name, emp_type
  </sql>
  <select id="getShopCustomer" resultType="com.express.yto.dto.ShopCustomerNameDTO">
    SELECT
      shop_name AS shopName,
      cust_name AS customerName
    FROM
      t_shop_emp
    GROUP BY
      shop_name,
      cust_name
      ORDER BY
      cust_name,shop_name
  </select>
  <select id="getEmpInfo" resultType="com.express.yto.dto.EmpBillInfoDTO">
    SELECT
      cust_name AS customerName,
      emp_name AS empName,
      emp_type AS empType
    FROM
        t_shop_emp
    WHERE
        emp_name IS NOT NULL
    AND emp_name &lt;> ''
    GROUP BY cust_name,emp_name,emp_type
  </select>
</mapper>