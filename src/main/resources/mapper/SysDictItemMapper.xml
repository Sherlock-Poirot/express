<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.express.yto.dao.SysDictItemMapper">

    <select id="selectByDictCode" resultType="com.express.yto.model.SysDictItem">
        SELECT * FROM t_sys_dict_item
        WHERE dict_code = #{dictCode} AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="selectByDictCodeAndValue" resultType="com.express.yto.model.SysDictItem">
        SELECT * FROM t_sys_dict_item
        WHERE dict_code = #{dictCode} AND dict_value = #{dictValue} AND status = 1
        LIMIT 1
    </select>

</mapper>
